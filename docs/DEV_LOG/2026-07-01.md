# 개발일지 — 2026-07-01 (화)

> 새 규칙(2026-07-01 도입): 매일 이 폴더에 파일 추가. 세션 이관 대응.
> 다음 세션 fresh Claude 는 반드시 이 파일과 `docs/CURRENT_STATE_260701.md` 먼저 읽을 것.

---

## 세션 컨텍스트
- David 님과 오랜 공백(5·6월) 이후 첫 세션
- 이관 문서 2건 공유받음 (`20260701_신규요청/` 폴더):
  - `CaaSWorks_CRM_통합_개발이관문서_260701.md` — STEP 0~7 로드맵
  - `DEVELOPMENT_NOTES.md` — **stale, 신뢰 금지**
- David 님 남은 근무시간 1시간 → **STEP 0 재기준화** 만 완결.

## 신규 규칙 3건 (메모리 저장 완료)
1. **매일 개발일지 작성** — 이 파일 형식
2. **세션 시작·종료 시 오늘 날짜 명시** — `📅 YYYY-MM-DD (요일)`
3. **데이터 원본은 엑셀** — Google Sheets 사용 금지. 사용자가 xlsx 공유.

## 오늘 한 일

### PR (예정: 이번 세션 종료 시)
- **docs/step0-rebaseline-260701** — STEP 0 실측 스크립트 + 재기준 문서 + 오늘 DEV_LOG

### 실측 결과 (`scripts/step0-measure.mjs` 결과)
- monthly_revenues 3,888행 / **2024·2025 = 0행** (완전 소실 확인)
- 2026 12개월 모두 데이터 있음 (5·6월 팀 입력 완료됨 — 이관 문서 표기와 다름)
- pipeline_leads 1,074건 (이탈 60% / 도입완료 17%)
- **site_sessions / pageviews / events 전부 0건** (트래킹 미유입)
- **audit_logs 0** (구현 자체가 없음, 미들웨어에 audit 로직 X)
- pipeline_leads 에 session_id / utm_* 컬럼 전무 (attribution 관통 불가)

### 매출 손실 원인 짚음 (자백)
- 2026-04-24 `scripts/split-projects-from-sheet.mjs --live` 가 원인 가능성 매우 높음
- 스크립트 흐름: 2026 revenues 삭제 + 백업 → **모든 projects 2,736개 삭제**
- `monthly_revenues.project_id` FK `ON DELETE CASCADE` → 2024/2025 매출까지 CASCADE 삭제
- 백업 JSON 에는 2026 만 저장됨 → 2024/2025 복구 불가
- 재발 방지: STEP 2 안전 동기화 규칙(upsert + audit + 전체 백업 + 트랜잭션) 필수

### Delete-all 패턴 목록 (STEP 2 리팩터 대상)
- ⚠️ **보호 없음**: `ga4-pages/route.ts:173`
- ✅ 보호됨 (data_source): naver-ads, google-ads, ga4-sources
- 폐기 대상: `split-projects-from-sheet.mjs`, `sync-april-*.mjs` (일회성 완료)

## 결정된 사항
- 데이터 원본은 David 님이 공유하는 엑셀 파일 (Google Sheets API 사용 안 함)
- 재기준 문서 위치: `caasworks/docs/CURRENT_STATE_260701.md` (레포 안, PR 리뷰 가능)
- STEP 1 진입 조건: 2024/2025 매출 엑셀 파일 수령 + 파일 구조 파악

## 미해결 / 다음 세션에서 이어받을 것

### 대기 (David 님 조치 필요)
- [ ] **2024/2025 매출 엑셀 파일** — Desktop/클로드/004_CRM 웹개발/20260701_신규요청/ 에 넣어주기
- [ ] 파일명 예시: `sales_2024.xlsx`, `sales_2025.xlsx` 또는 통합
- [ ] 엑셀 헤더 구조 (연도/월/회사명/현장/서비스/금액 컬럼 위치)

### 다음 세션 즉시 할 것 (엑셀 도착 시)
1. 엑셀 파일 파싱 (`xlsx` 스킬)
2. 라이브 DB 와 대조 (회사명 매칭, 현장·서비스 키)
3. **upsert 전용** 임포트 스크립트 작성 (DELETE 절대 없이)
4. 전체 monthly_revenues 백업 → JSON
5. DRY-RUN 결과 리포트 → David 님 승인 → LIVE
6. 실행 후 연도별 행수·합계 검증

### STEP 2 이후 (블록됨 — STEP 1 완료 후)
- audit_logs DB trigger 구현
- ga4-pages 동기화 데이터 보호 (data_source 컬럼)
- pipeline_leads 에 session_id/utm_* 컬럼 추가 (attribution)
- tracking.js 실제 설치 상태 확인 (왜 site_sessions=0 인지)

## 파괴적 작업 (오늘)
- **없음**. 오늘은 read-only.

## 관련 파일
- `scripts/step0-measure.mjs` — 재실행 가능 실측 스크립트
- `docs/CURRENT_STATE_260701.md` — 재기준 문서
- 이관 문서 원본: `../../20260701_신규요청/CaaSWorks_CRM_통합_개발이관문서_260701.md`

---
📅 세션 종료 시각: 2026-07-01 (화) 퇴근 전
